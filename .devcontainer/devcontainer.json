// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/ruby

// NOTE: il faut clone en conservant les fins de ligne LF (git config --global core.autocrlf false)
// c'est l'objet du .gitattributes dans le repo
{
	"name": "Ruby",
	"image": "mcr.microsoft.com/devcontainers/ruby:3.1-bookworm",

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},


	// Use 'postCreateCommand' to run commands after the container is created.
	"postCreateCommand": "pwd ; sudo apt-get update && DEBIAN_FRONTEND=noninteractive sudo apt-get install -y libffi-dev gcc libvips libvips-tools ; gem install jekyll-admin-0.12.0.gem ; BUNDLE_GEMFILE=Gemfile-gem-admin bundle install",
	"postStartCommand": "sh container-start.sh",

	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	"forwardPorts": [4000]
}
